name: facebook_main_view_lego
description: "Facebook Main View Lego by June"
#publish_to: 'none'
repository: https://github.com/flutter-lego/facebook_main_view_lego

version: 1.0.0

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  june_flow_util:
  styled_widget: #@add
  modal_bottom_sheet: #@add
  flutter_svg: #@add

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:
  assets:
    - assets/lego/facebook_main_view_lego/
  uses-material-design: true

topics:
  - lego